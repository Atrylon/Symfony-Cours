{% extends 'base.html.twig' %}

{% block title %}Hello !{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    {{ form_start(form) }}
    Firstname : {{  form_widget(form.firstname) }}
    <br />
    Lastname : {{  form_widget(form.lastname) }}
    <br />
    Email : {{  form_widget(form.email) }}
    <br />
    Birthday : {{  form_widget(form.birthday.day) }}
    {{  form_widget(form.birthday.month) }}
    {{  form_widget(form.birthday.year) }}
    {{ form_end(form) }}

</div>

    <ul>
        {% for user in users %}
        <li>{{ user.firstname }} {{ user.lastname }} - {{ user.email}} - né le {{ user.birthday|date('d/m/y') }}</li>
        {% endfor %}
    </ul>
{% endblock %}
